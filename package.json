{
  "name": "friday-inbox",
  "private": true,
  "version": "1.0.0",
  "description": "Friday's email inbox - Cloudflare Email Worker with KV storage and Telegram notifications",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "postal-mime": "^2.4.1"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
